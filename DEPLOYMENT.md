# Деплой на GitHub Pages

Этот проект настроен для автоматического деплоя на **GitHub User Pages** через GitHub Actions.

## 🌟 О GitHub User Pages

Репозиторий `taravkov.github.io` это специальный тип GitHub Pages:
- ✅ Бесплатный хостинг
- ✅ Корневой домен: `https://taravkov.github.io`
- ✅ Кастомный домен: `https://cj-t.ee` 
- ✅ Один User Pages на аккаунт
- ✅ Автоматический деплой из `main` ветки
- ✅ Автоматический HTTPS для кастомного домена

## 🚀 Первоначальная Настройка

### 1. Репозиторий Готов

Репозиторий уже настроен как User Pages:
```bash
git remote add origin https://github.com/taravkov/taravkov.github.io.git
```

> **Важно:** Для User Pages `vite.config.js` уже настроен с `base: '/'`

### 2. Включение GitHub Pages

1. **Перейди в Settings репозитория**
   - Открой https://github.com/taravkov/taravkov.github.io
   - Нажми `Settings` (⚙️)

2. **Настрой GitHub Pages**
   - В левом меню выбери `Pages`
   - В разделе `Source` выбери **`GitHub Actions`**
   - Сохрани изменения

> **Примечание:** Для User Pages (`username.github.io`) GitHub Pages включается автоматически!

### 3. Первый Деплой

```bash
# Добавь все файлы
git add .

# Создай коммит
git commit -m "Add GitHub Pages deployment"

# Запуш в main ветку
git push origin main
```

После пуша:
- GitHub Actions автоматически запустит сборку
- Проект будет задеплоен на GitHub Pages
- Сайт будет доступен по адресам:
  - **`https://taravkov.github.io`** (основной GitHub URL)
  - **`https://cj-t.ee`** (кастомный домен)

## 📋 Проверка Деплоя

### Смотреть статус деплоя

1. Перейди во вкладку **Actions** в твоем репозитории
2. Найди последний workflow run "Deploy to GitHub Pages"
3. Кликни на него чтобы увидеть детали

### Если деплой успешен

- ✅ Зеленая галочка рядом с коммитом
- Сайт доступен по URLs:
  - **`https://taravkov.github.io`**
  - **`https://cj-t.ee`** (кастомный домен)

### Если деплой провалился

- ❌ Красный крестик рядом с коммитом
- Открой детали в Actions чтобы увидеть ошибку
- Наиболее частые проблемы:
  - Ошибки сборки (проверь локально: `npm run build`)
  - Не выбран источник "GitHub Actions" в Pages настройках
  - Проблемы с правами доступа (проверь permissions в workflow)

## 🔄 Автоматический Деплой

После первоначальной настройки, каждый раз когда ты пушишь в `main`:

```bash
git add .
git commit -m "Update visuals"
git push origin main
```

GitHub Actions автоматически:
1. Установит зависимости (`npm ci`)
2. Соберет проект (`npm run build`)
3. Задеплоит на GitHub Pages

Весь процесс занимает 1-2 минуты.

## 🎯 Ручной Деплой

Можно также запустить деплой вручную:

1. Перейди во вкладку **Actions**
2. Выбери workflow "Deploy to GitHub Pages"
3. Нажми **Run workflow**
4. Выбери ветку `main`
5. Нажми **Run workflow**

## 🛠️ Локальная Проверка Сборки

Перед пушем можно проверить сборку локально:

```bash
# Собрать проект
npm run build

# Проверить собранные файлы
npm run preview
```

Preview сервер запустится на `http://localhost:4173`

## 📝 Структура Workflow

Файл `.github/workflows/deploy.yml` содержит:

### Триггеры
- `push` в ветку `main` - автоматический деплой
- `workflow_dispatch` - ручной запуск

### Jobs

**Build:**
- Checkout кода
- Установка Node.js 20
- Установка зависимостей
- Сборка проекта
- Загрузка артефактов

**Deploy:**
- Деплой собранных файлов на GitHub Pages
- Публикация URL сайта

## 🐛 Troubleshooting

### Белый экран после деплоя

**Проблема:** Ресурсы не загружаются

**Решение:** 
- Для User Pages `base` должен быть `'/'` (уже настроено)
- Очисти кэш браузера (Ctrl+Shift+R)
- Подожди 2-3 минуты после первого деплоя

### 404 ошибка

**Проблема:** Страница не найдена

**Решение:** 
- Убедись что GitHub Pages включен в настройках
- Проверь что выбран источник "GitHub Actions"
- Подожди несколько минут после первого деплоя

### Build fails

**Проблема:** Ошибка при сборке в Actions

**Решение:**
1. Проверь локально: `npm run build`
2. Убедись что все зависимости в `package.json`
3. Проверь логи в Actions для деталей

## 📊 Мониторинг

После деплоя можно отслеживать:
- **Traffic:** Settings → Insights → Traffic
- **Pages:** Settings → Pages (показывает URL и статус)
- **Actions:** Вкладка Actions (история деплоев)

## 🎨 Обновление Визуализации

Workflow для обновления:

```bash
# 1. Редактируй код
# 2. Проверь локально
npm run dev

# 3. Проверь сборку
npm run build
npm run preview

# 4. Если все ок - пуш
git add .
git commit -m "Update: описание изменений"
git push origin main

# 5. Жди 1-2 минуты и проверяй на GitHub Pages!
```

---

## 🌟 Твой Сайт

После деплоя визуализация будет доступна по адресам:

**Основной GitHub URL:**
```
https://taravkov.github.io
```

**Кастомный домен:**
```
https://cj-t.ee
```

Оба URL ведут на один и тот же сайт! 🎉

## 🔧 Настройка DNS для cj-t.ee

Чтобы кастомный домен работал, нужно настроить DNS у регистратора домена:

### Для Apex домена (cj-t.ee):

Добавь 4 A Records:
```
Тип: A    Имя: @    Значение: 185.199.108.153
Тип: A    Имя: @    Значение: 185.199.109.153
Тип: A    Имя: @    Значение: 185.199.110.153
Тип: A    Имя: @    Значение: 185.199.111.153
```

### Опционально - WWW субдомен:

```
Тип: CNAME    Имя: www    Значение: taravkov.github.io
```

### Проверка DNS

После настройки DNS подожди **10-30 минут** для распространения изменений.

Проверить можно командой:
```bash
dig cj-t.ee +short
# Должны вернуться IP адреса GitHub Pages
```

Или онлайн: https://dns.google.com

### Включение HTTPS

После успешного деплоя и настройки DNS:

1. Перейди в **Settings → Pages** на GitHub
2. Дождись проверки домена (статус: "DNS check successful")
3. Включи **"Enforce HTTPS"**
4. GitHub автоматически выпустит SSL сертификат от Let's Encrypt (занимает 5-10 минут)

После этого `https://cj-t.ee` будет работать с защищенным соединением! 🔒

---

## 🔗 Полезные Ссылки

- [Vite Deployment Guide](https://vitejs.dev/guide/static-deploy.html)
- [GitHub Pages Documentation](https://docs.github.com/en/pages)
- [GitHub Actions Documentation](https://docs.github.com/en/actions)
- [Managing Custom Domains](https://docs.github.com/en/pages/configuring-a-custom-domain-for-your-github-pages-site)

Готово! Твоя визуализация будет доступна всему миру на `cj-t.ee`! 🌍✨

